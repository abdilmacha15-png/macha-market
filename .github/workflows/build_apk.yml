name: Build Macha Market APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      # 1. Checkout
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Find ZIP
      - name: Find Macha Market ZIP
        shell: bash
        run: |
          set -e

          echo "===== Repository files ====="
          find . -maxdepth 3 -type f | sort | head -200

          ZIP_FILE=$(find . -maxdepth 2 -type f \
            -iname "macha_market_storage_fix.zip" \
            -print -quit)

          if [ -z "$ZIP_FILE" ]; then
            echo "ERROR: macha_market_storage_fix.zip not found"
            exit 1
          fi

          echo "ZIP FOUND: $ZIP_FILE"

          echo "ZIP_FILE=$ZIP_FILE" >> "$GITHUB_ENV"

      # 3. Unzip
      - name: Unzip Macha Market
        shell: bash
        run: |
          set -e

          rm -rf extracted
          mkdir -p extracted

          unzip -o "$ZIP_FILE" -d extracted

          echo "===== Extracted project ====="
          find extracted -maxdepth 4 -name "pubspec.yaml" -print

      # 4. Find Flutter project
      - name: Find Flutter project
        shell: bash
        run: |
          set -e

          PUBSPEC=$(find extracted \
            -type f \
            -name "pubspec.yaml" \
            -print -quit)

          if [ -z "$PUBSPEC" ]; then
            echo "ERROR: No pubspec.yaml found"
            exit 1
          fi

          PROJECT_DIR=$(dirname "$PUBSPEC")

          echo "Flutter project:"
          echo "$PROJECT_DIR"

          if [ ! -d "$PROJECT_DIR/android" ]; then
            echo "ERROR: Android folder not found"
            exit 1
          fi

          echo "PROJECT_DIR=$PROJECT_DIR" >> "$GITHUB_ENV"

      # 5. Java 17
      # IMPORTANT: this comes AFTER the ZIP has been extracted
      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      # 6. Flutter
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.0'
          channel: stable
          cache: true

      # 7. Flutter version
      - name: Flutter version
        run: |
          flutter --version
          dart --version
          java -version

      # 8. Dependencies
      - name: Flutter pub get
        working-directory: ${{ env.PROJECT_DIR }}
        run: |
          flutter pub get

      # 9. Fix minSdk
      - name: Fix Android minSdkVersion
        working-directory: ${{ env.PROJECT_DIR }}
        shell: bash
        run: |

          echo "===== BEFORE FIX ====="

          grep -R -n -E \
            "minSdkVersion|minSdk|flutter.minSdkVersion" \
            android || true

          echo "===== APPLYING MIN SDK 23 ====="

          # Groovy Gradle
          find android -type f -name "*.gradle" -exec sed -i \
            -E 's/minSdkVersion[[:space:]]*flutter\.minSdkVersion/minSdkVersion 23/g' {} \;

          find android -type f -name "*.gradle" -exec sed -i \
            -E 's/minSdkVersion[[:space:]]*21/minSdkVersion 23/g' {} \;

          find android -type f -name "*.gradle" -exec sed -i \
            -E 's/minSdk[[:space:]]*=[[:space:]]*flutter\.minSdkVersion/minSdk = 23/g' {} \;

          find android -type f -name "*.gradle" -exec sed -i \
            -E 's/minSdk[[:space:]]*=[[:space:]]*21/minSdk = 23/g' {} \;

          # Kotlin Gradle
          find android -type f -name "*.gradle.kts" -exec sed -i \
            -E 's/minSdk[[:space:]]*=[[:space:]]*flutter\.minSdkVersion/minSdk = 23/g' {} \;

          find android -type f -name "*.gradle.kts" -exec sed -i \
            -E 's/minSdk[[:space:]]*=[[:space:]]*21/minSdk = 23/g' {} \;

          # Manifest
          find android -type f -name "AndroidManifest.xml" -exec sed -i \
            -E 's/android:minSdkVersion="21"/android:minSdkVersion="23"/g' {} \;

          echo "===== AFTER FIX ====="

          grep -R -n -E \
            "minSdkVersion|minSdk|flutter.minSdkVersion" \
            android || true

      # 10. Clean
      - name: Flutter clean
        working-directory: ${{ env.PROJECT_DIR }}
        run: |
          flutter clean
          flutter pub get

      # 11. Analyze
      - name: Flutter analyze
        working-directory: ${{ env.PROJECT_DIR }}
        run: |
          flutter analyze || true

      # 12. Build APK
      - name: Build release APK
        working-directory: ${{ env.PROJECT_DIR }}
        run: |
          flutter build apk \
            --release \
            --no-tree-shake-icons

      # 13. Verify APK
      - name: Verify APK
        working-directory: ${{ env.PROJECT_DIR }}
        shell: bash
        run: |

          APK=$(find build/app/outputs/flutter-apk \
            -type f \
            -name "*.apk" \
            -print -quit)

          if [ -z "$APK" ]; then
            echo "ERROR: APK was not created"
            exit 1
          fi

          echo "======================================"
          echo "🎉 MACHA MARKET APK SUCCESSFULLY BUILT"
          echo "======================================"

          ls -lh "$APK"

      # 14. Upload APK
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: macha-market-release-apk
          path: ${{ env.PROJECT_DIR }}/build/app/outputs/flutter-apk/*.apk
          if-no-files-found: error
